@page "/"

<h1>Hello, world!</h1>

Loading time: @_elapsed milliseconds

@code
{
    long _elapsed;

    protected override void OnAfterRender(bool firstRender)
    {
        if (firstRender)
        {
            _elapsed = TimeStateContainer.Stop();
            StateHasChanged();
        }
    }
}
